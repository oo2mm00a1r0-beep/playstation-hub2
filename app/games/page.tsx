import { Catalog } from '@/components/catalog';

export default function GamesPage() {
  return <Catalog category="Games" title="Games" subtitle="New and used titles across every PlayStation generation" />;
}
